<UserControl x:Class="WorldCup2026.Views.GroupStageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:WorldCup2026.Converters"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1100">

    <UserControl.Resources>
        <converters:TeamCodeToFlagConverter x:Key="Flag" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyState" Grid.Row="0"
                   Text="Waiting for tournament data..."
                   FontSize="16" Foreground="Gray"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20" />

        <!-- Groups TabControl -->
        <TabControl x:Name="GroupsTabControl" Grid.Row="0"
                    Visibility="Collapsed"
                    Margin="0,0,0,4"
                    BorderBrush="#e0e0e0"
                    Background="White">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="FontSize" Value="12" />
                    <Setter Property="Padding" Value="10,4" />
                    <Setter Property="MinWidth" Value="48" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="TabBorder"
                                        Background="White"
                                        BorderBrush="#ddd"
                                        BorderThickness="1,1,1,0"
                                        CornerRadius="4,4,0,0"
                                        Padding="{TemplateBinding Padding}"
                                        Margin="0,2,1,0">
                                    <ContentPresenter ContentSource="Header" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="Background" Value="#1a365d" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
        </TabControl>

        <!-- Matches for selected group -->
        <Border x:Name="MatchesPanel" Grid.Row="1"
                Visibility="Collapsed"
                Background="White"
                BorderBrush="#e0e0e0"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,4,0,0">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="Group Matches"
                           FontSize="14" FontWeight="Bold"
                           Foreground="#1a365d" Margin="0,0,0,8" />
                <ItemsControl x:Name="MatchesList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="6,4" BorderBrush="#f0f0f0" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Home: flag + name (right-aligned) -->
                                    <StackPanel Grid.Column="0" Orientation="Horizontal"
                                                HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <ContentPresenter Content="{Binding HomeTeamCode, Converter={StaticResource Flag}}"
                                                          VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBlock Text="{Binding HomeTeamName}"
                                                   FontSize="12" VerticalAlignment="Center" />
                                    </StackPanel>

                                    <!-- Score -->
                                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                VerticalAlignment="Center" Margin="12,0">
                                        <TextBlock Text="{Binding HomeScore}" FontSize="13" FontWeight="Bold"
                                                   Width="22" TextAlignment="Center" />
                                        <TextBlock Text="-" FontSize="13" Foreground="Gray"
                                                   Width="12" TextAlignment="Center" />
                                        <TextBlock Text="{Binding AwayScore}" FontSize="13" FontWeight="Bold"
                                                   Width="22" TextAlignment="Center" />
                                    </StackPanel>

                                    <!-- Away: name + flag (left-aligned) -->
                                    <StackPanel Grid.Column="2" Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding AwayTeamName}"
                                                   FontSize="12" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <ContentPresenter Content="{Binding AwayTeamCode, Converter={StaticResource Flag}}"
                                                          VerticalAlignment="Center" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
